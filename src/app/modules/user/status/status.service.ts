import { StatusCodes } from 'http-status-codes';
import ApiError from '../../../../errors/ApiError';
const getAllStatusFromDB = async (year: number) => {
  return {};
};
export const StatusService = {
  getAllStatusFromDB,
};
